#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int n;
    do
    {
        n = get_long("Number: ");
    }
    // Reject anything else besides numbers between 1 and 8
    while (len(n) < );
}