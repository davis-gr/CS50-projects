#include <cs50.h>
#include <stdio.h>

int count_letters(string text);

int main(void)
{
    string fullText = get_string("Text: ");
    printf("%s", fullText)
}

int count_letters(string text)
{
    
}